# React lab

Interactive Vite example for the shared React package. It demonstrates application switching, approvals, questions and trusted custom surfaces for ACV2, content and analytics.

```bash
pnpm install
pnpm --dir examples/react-lab dev
```

The fixtures are local and do not perform network or production actions.
